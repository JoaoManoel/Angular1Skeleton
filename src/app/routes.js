(function() {
  'use strict';

  angular
    .module('app')
    .config(configBlock)
    .run(runBlock);

    configBlock.$inject = [];

    runBlock.$inject = [];

    function configBlock() {
    }

    function runBlock() {
    }

})();
